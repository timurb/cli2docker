# Tasks

## Plan
- [x] Review change spec and confirm scenarios cover prefix behavior.
- [x] Implement `--image-prefix` flag and default (`cli/`) for `build`.
- [x] Apply prefix only when `--image` is derived from `--package`.
- [x] Emit warning when `--image` is explicit and prefix is ignored.
- [x] Update tests to cover default and scoped package cases.
- [x] Run tests.
