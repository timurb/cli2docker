## 1. Dockerfile generation

- [x] 1.1 Add Bun global install env vars to Dockerfile output (`BUN_INSTALL_GLOBAL_DIR`, `BUN_INSTALL_BIN`) for Bun builds.
- [x] 1.2 Keep npm Dockerfile output unchanged.

## 2. Tests

- [x] 2.1 Update Dockerfile content tests to assert Bun env vars are present.

## 3. Docs

- [x] 3.1 Update README or usage notes to mention Bun global install env vars if needed.
